on:
  push:
    paths:
      - 'vanilla/**'
      - '.github/workflows/abstract.yml'
      - '.github/workflows/vanilla.yml'
  workflow_dispatch:
jobs:
  build:
    uses: ./.github/workflows/abstract.yml
    with:
      folder: vanilla
        
